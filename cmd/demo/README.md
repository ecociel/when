# Scheduler nd Worker with seperate Observer

This is an app that is running a scheduler and a worker but not an observer. The
observer must be run as a seperate application by starting the observer example.


